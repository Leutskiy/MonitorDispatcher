/* 1 */
Кнопка закрыть - не сбрасывает ничего (для локального режима)

/* 2 */
Кнопка ресет и ON/OFF радио баттон выдают сообщение о выборе одного из режимов - плохо

/* 3 */
В локальном режиме есть такая ситуация:

- включаем видео ---> переходим на влкадку отправки сообщения ---> отправляем сообщение ---> возвращаемся на вкладку видео и видим, что кнопка с плей не перешла состояние выкл.

/* 4 */ 
Долго работает переход из удаленного режим в локальный (надо осторожнее с использованием асинхронных методов, которые работают по сети... там проблема именно в таких методах, нужно организовать более безошибочную их работу)

/* 5 */
Событие отображение скролла, то сдвигаем столбец с кнопками влево

/* 6 */
Дизайн вкладки с сообщениями

/* 7 */
Видео в полноэкранном режиме

/* 8 */
Когда закрываю ресивер, то нужно видео/музыку/звуки возвращать в исходное состояние (кнопки)

/* 9 */
Убрать контекстное меню у плеера - протестить появление на удаленной и локальной версии

/* 10 */
В свернутом виде отображается видео

/* 11 */
Когда в удаленном выключаешь, то нужно все прекратить на ресивере

/* 12 */
Отловить ошибку - хост принудительно разорвал соединение (IOException)

/* 13 */
Не всегда отображается сообщение, которое было выделено

/* 14 */
Кнопка Accept в настройках не должна нажиматься... На ресивер уже попадают все из настроек!

/* 15 */
Во время просмотра видео игнорировать нажатие любых клавиш и клваиш мыши

!!! Какая-то путаница с проставлением состояний видео плеера, так как  не всегда закрывает окно с фотокой

/* 16 */
Иногда появляется ощибка при сбросе всех режимов, когда стоим на удаленном.